<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hp.dao.ShiPinDao">
    <insert id="insertUrl" parameterType="com.hp.pojo.ShiPin" >
        insert into video (url,name,lujing,filetype) values (#{url},#{name},#{lujing},#{filetype})
    </insert>
    
    <select id="selectShipinCount" parameterType="com.hp.utils.Info" resultType="Integer">
        select count(1) from video
        <where>
            <if test="filetype!=null and filetype!=''">
                filetype=#{filetype}
            </if>
        </where>
    </select>
    
    <select id="selAllOrders" parameterType="com.hp.utils.Info" resultType="com.hp.pojo.ShiPin">
        select * from video
        <where>
            <if test="filetype!=null and filetype!=''">
                filetype=#{filetype}
            </if>
        </where>
         limit #{startRow},#{size}
    </select>
</mapper>